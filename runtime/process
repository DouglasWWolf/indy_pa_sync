    // Finally, initialize phased array mode.  See lib_GeChip.py, function initPAMode().
    /// Some of this is documented in FPGA doc section 10.5.  Order of operations follows labcodes rather than FPGA doc.
    /// Sensor Chip Setup, documented in 10.5.1.  Initialize chip to operate in phased-array mode. 
    //  Enable sync pulse from chip by setting GPIO bit 11 to 0. This syncs control signals between FPGA and chip.
    constexpr auto selectRegval = 0x7E0;
    constexpr auto directionRegval = 0xFE0;
    constexpr auto syncinRegval = 0xF002;
    fpga_.setChipRegister(GPIO_CTRL_select, selectRegval);
    fpga_.setChipRegister(GPIO_CTRL_direction, directionRegval);
    fpga_.setChipRegister(GPIO_SYNC_syncin, syncinRegval);
    fpga_.setChipRegister(SRDWR_PA_MODE_pa_mode, 1);
    fpga_.setChipRegister(SRDWR_PA_MODE_src_sel, 1);
    fpga_.setChipRegister(SRDWR_VLIQ_PERIOD_dk_period, 0);
    fpga_.setChipRegister(SRDWR_VLIQ_PERIOD_br_period, 0);
